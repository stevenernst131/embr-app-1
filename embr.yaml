components:
  - name: web
    rootDirectory: web
    role: static
    path: /

  - name: api
    rootDirectory: services/api
    role: web
    platform: nodejs
    path: /api
    run:
      port: 8080
      start: npm start
      healthCheckPath: /api/health

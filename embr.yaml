version: 1
platform: nodejs
platformVersion: "24"
run:
  port: 3000
  startCommand: node server.js

dependencies:
  app-database:
    type: postgres
    description: "Main application database"
    required: true
    provides:
      - name: DATABASE_URL
        secret: true
        scope: runtime
    postgres:
      framework: raw